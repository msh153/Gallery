<Window x:Class="Gallery.frmViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gallery"
        mc:Ignorable="d"
        Title="frmViewer" Height="444" Width="794" Loaded="WindowLoaded">

    <Grid>
        <Image x:Name="ViewedPhotoForSlideShow" Height="Auto" Margin="41,10,38,62"/>
        <Button x:Name="buttonNext" Content="Button" HorizontalAlignment="Left" Margin="628,376,0,0" VerticalAlignment="Top" Width="75" Click="Button_Next_Click">
            <Button.Triggers>
                <!-- Animates the rectangle's opacity. -->
                <EventTrigger RoutedEvent="Button.Click">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
            Storyboard.TargetName="ViewedPhotoForSlideShow" 
            Storyboard.TargetProperty="Opacity"
            From="0.0" To="1.0" Duration="0:0:2" 
            AutoReverse="False" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Button.Triggers>

        </Button>


    </Grid>
</Window>
